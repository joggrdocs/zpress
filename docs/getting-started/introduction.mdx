---
title: Introduction
description: What zpress is, why it exists, and what it gives you out of the box.
---

import { FeatureCard, FeatureGrid, Icon } from '@zpress/ui/theme'

# Introduction

## What is zpress

zpress is a documentation framework for monorepos. Your config file _is_ the information architecture — it defines what content exists, where it comes from, and how it's organized. Source files are never modified. zpress syncs them into a build directory, injects frontmatter, generates sidebars and navigation, and outputs a static site.

```bash
npx @zpress/kit dev
```

That's it. One command, working docs site.

## Why zpress

There are great documentation tools — [VitePress](https://vitepress.dev), [Rspress](https://rspress.dev), [Docusaurus](https://docusaurus.io). They inspired this project. So why build another one?

**Zero effort.** Most doc tools need significant setup before they produce anything useful — plugins, theme wiring, sidebar config, restructuring your files. zpress ships with perfect defaults that match the 80% use case. Put markdown in `docs/`, run one command, get a working site with sidebar, navigation, and themes.

**Your structure, not ours.** The config maps to how you already organize your markdown. You describe where your files are and how they should be grouped. You don't restructure your docs to fit the tool — the tool fits your docs.

**Built for the vibe.** AI coding tools are everywhere. When you're vibing on a project — spinning up features with Claude, Copilot, Cursor — documentation shouldn't slow you down. Tell your AI to write markdown, run `zpress dev`, done. No plugin config, no theme wiring, no boilerplate.

## Features

<FeatureGrid>
  <FeatureCard
    title="Zero Effort"
    description="No restructuring, no plugins, no theme wiring. Point it at markdown and ship."
    href="/getting-started/quick-start"
    icon={<Icon icon="pixelarticons:speed-fast" />}
    iconColor="purple"
    span={4}
  />
  <FeatureCard
    title="Your Structure"
    description="Config maps to how you already organize markdown. The tool fits your docs."
    href="/concepts/sections-and-pages"
    icon={<Icon icon="pixelarticons:layout" />}
    iconColor="blue"
    span={4}
  />
  <FeatureCard
    title="Built for AI"
    description="Tell your AI to write markdown, run one command, ship a docs site."
    href="/concepts/auto-discovery"
    icon={<Icon icon="pixelarticons:zap" />}
    iconColor="green"
    span={4}
  />
  <FeatureCard
    title="Monorepo Native"
    description="First-class workspace support with isolated sidebars and landing pages."
    href="/concepts/workspaces"
    icon={<Icon icon="pixelarticons:git-merge" />}
    iconColor="amber"
    span={4}
  />
  <FeatureCard
    title="Themes & Dark Mode"
    description="Three built-in themes with dark mode, color overrides, and a runtime switcher."
    href="/concepts/themes"
    icon={<Icon icon="pixelarticons:paint-bucket" />}
    iconColor="pink"
    span={4}
  />
  <FeatureCard
    title="Auto Navigation"
    description="Sidebar and nav bar generated from your config. Glob discovery, sorting, nesting."
    href="/concepts/navigation"
    icon={<Icon icon="pixelarticons:list-box" />}
    iconColor="cyan"
    span={4}
  />
</FeatureGrid>
